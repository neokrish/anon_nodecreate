This Drupal module allows anonymous users to post content with a user-login/register form attached to the node form. Hence the node created is either attached to the newly created user or the logged in user.
